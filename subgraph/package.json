{
  "name": "depositContract",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:polygon": "graph build",
    "build:sepolia": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ depositContract",
    "deploy:polygon": "graph deploy --node https://api.studio.thegraph.com/deploy/ depositContract-polygon --network polygon",
    "deploy:sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ depositContract-sepolia --network sepolia",
    "create-local": "graph create --node http://localhost:8020/ depositContract",
    "remove-local": "graph remove --node http://localhost:8020/ depositContract",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 depositContract",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
